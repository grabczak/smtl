# Theory: QF_LIA or QF_NIA
set logic QF_NIA

# Declarations
var a, b, x, y, d : int

# Assignments
let bezout = a * x + b * y == d

# Assertions
assert a == 12 /\ b == 18
assert d >= 0
assert a % d == 0 /\ b % d == 0
assert bezout

# Output commands
check sat
get model
exit
